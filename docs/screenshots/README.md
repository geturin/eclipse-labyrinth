# 截图说明

原始仓库中的六张 title/exploration/battle PNG 是 v0.1 的画面记录，不是 v0.2 规则证明。
运行 `python tests/browser_smoke.py` 会在本机覆盖它们，并生成 v0.2 Boss 的桌面/手机截图。
截图不属于游戏运行依赖；当前构建仍是纯单文件、程序化素材。

浏览器测试结果在 `../browser-smoke-results.json`；测试范围与限制见 `../QA.md`。
